{
  "name": "fileshare-render",
  "version": "1.0.0",
  "description": "Render deployment configuration for FileShare P2P platform",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm start"
  }
}